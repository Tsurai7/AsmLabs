;hello2.asm
    .model small
    .stack 100h

.data
message db "Array of c h a r s",0Dh,0Ah,'$'
    
.code
start: mov ax, @data
       mov ds, ax
       mov dx, offset message
       mov ah, 9
       int 21h   
       
       mov ah, 4Ch
       int 21h  
end start    
